<?xml version="1.0" encoding="utf-8" ?>
<views:BasePage
    xmlns:views="clr-namespace:Sample.Views"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:Sample.ViewModels"
    x:Class="Sample.Views.MediaFileInfoPage"
    x:DataType="vm:MediaFileInfoVM"
    Title="{Binding File.FileName}">

    <ScrollView>
        <StackLayout>
            <views:MediaFileView File="{Binding File}" FilePath="{Binding Path}"
                                 HeightRequest="400"/>

            <Label Margin="0,8,0,0" FontAttributes="Bold" Text="Name:" />
            <Label Text="{Binding File.FileNameWithoutExtension}" />

            <Label Margin="0,8,0,0" FontAttributes="Bold" Text="Extension:" />
            <Label Text="{Binding File.Extension}" />

            <Label Margin="0,8,0,0" FontAttributes="Bold" Text="Metadata:" />
            <Label Text="{Binding Metadata}" LineBreakMode="WordWrap" />
        </StackLayout>
    </ScrollView>
</views:BasePage>
